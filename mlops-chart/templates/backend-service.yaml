apiVersion: v1

kind: Service

metadata:
  name: backend
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: {{ .Values.backend.name }}

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP