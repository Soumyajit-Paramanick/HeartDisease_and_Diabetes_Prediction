apiVersion: v1

kind: Service

metadata:
  name: backend
  namespace: {{ .Values.namespace }}

  labels:
    app: backend

spec:

  selector:
    app: {{ .Values.backend.name }}

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP