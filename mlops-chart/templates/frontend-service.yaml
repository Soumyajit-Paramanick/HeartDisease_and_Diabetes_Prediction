apiVersion: v1

kind: Service

metadata:
  name: frontend
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: {{ .Values.frontend.name }}

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP